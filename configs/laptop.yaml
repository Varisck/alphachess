storage:
  root_uri: "file:///t:/Progetti/alphachess/data"

nn:
  num_blocks: 5
  channels: 64

mcts:
  num_simulations: 200

selfplay:
  games_per_worker_batch: 4

pretrain:
  moves_per_game: 5
  max_positions: 4_000_000
  records_subdir: "pretrain_records"

train:
  batch_size: 256
  min_games_before_training: 50
  steps_per_checkpoint: 200

orchestrator:
  role: local
  selfplay_workers: 2
